# VBS translation layer

## Notes

- Maybe a good idea to overcome the problem of having the model parameters as values of the objects is to pass the object itself (now Im just writing the string). But for that I need to be able to compose them and to handle the math operators too..
- Also I depend a lot on the user to put correctly the values.. Currently the code will be generated even if it doesnt work at all..


